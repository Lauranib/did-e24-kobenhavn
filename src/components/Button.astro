---

// Henter parametrene fra Astro.props

const { variant= "primary", size = "medium" } = Astro.props; 

 
//Definer den styling (Tailwind classes, der gør sig gældende for alle knapper

const buttonStyling = "border-2 rounded-full hover:transition duration-300"; 

 
//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige varianter 

const variantClasses = {

"primary": "bg-slate-900  text-white hover:bg-slate-100 hover:text-slate-900 hover: border-slate-900",
"secondary": "bg-slate-100 text-slate-900 border-slate-900 hover:bg-slate-900 hover:text-white"

};

 
//Definer den styling (Tailwind classes), der gør sig gældende for de forskelligeog størrelser.

const sizeClasses = {

"large": "px-12 py-4 text-xl",
"medium": "px-7 py-3 text-lg",
"small": "px-3 py-2 text-base"

};

---

<button class:list={[buttonStyling, variantClasses[variant], sizeClasses[size]]}><slot /></button>

<!-- <div>
    <button class="bg-slate-900  text-white rounded-full text-lg hover:bg-slate-100 hover:text-slate-900 hover: border-slate-900 hover:transition duration-150"><slot /></button>
</div> -->